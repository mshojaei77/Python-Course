# Write a Python function that takes two integer parameters and returns a complex number consisting of those parameters as the real and imaginary parts, respectively.
real = int(input("enter the real part: "))
img = int(input("enter the imagin part: "))
print(complex(real,img))
