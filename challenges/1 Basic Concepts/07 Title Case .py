# Write a program that takes a string and converts it into "title case" (i.e., the first letter of every word is capitalized).
string_input = input("Enter a string: ")
title_case = string_input.title()
print(title_case)
