# Develop a program that takes a string as input and outputs the reverse of the string.
stringInput = input("Enter a string: ")
print(f"Original string: {stringInput}")
reverseString = stringInput[::-1]
print(f"Reversed string: {reverseString}")
